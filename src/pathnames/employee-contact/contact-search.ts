const main = {
    name: 'Contact Search',
    path: '/contact-search'
};

const detail = {
    name: 'Contact Search Detail',
    path: '/contact-search-detail'
};

const resume = {
    name: 'Contact Search Resume',
    path: '/contact-search-resume'
};

const employeeSearch = {
    main,
    detail,
    resume
};

export default employeeSearch;
