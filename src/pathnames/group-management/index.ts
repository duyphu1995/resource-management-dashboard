import unitHistoryInfo from './unit-history-info';

const main = {
    name: 'Group Management',
    path: '/group-management'
};

const detail = {
    name: 'Employee Detail',
    path: '/employee-detail'
};

const openReportList = {
    name: 'Report List Management',
    path: '/report-list-group-management'
};

const groupManagement = {
    main,
    detail,
    unitHistoryInfo,
    openReportList
};

export default groupManagement;
