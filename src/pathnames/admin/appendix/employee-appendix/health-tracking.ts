const main = {
    name: 'Health Tracking',
    path: '/health-tracking'
};

const detail = {
    name: 'Health Tracking Details',
    path: '/health-tracking-detail'
};

const add = {
    name: 'Add New Health Tracking',
    path: '/health-tracking-add'
};

const edit = {
    name: 'Edit Health Tracking',
    path: '/health-tracking-edit'
};

const healthTracking = {
    main,
    detail,
    add,
    edit,
};

export default healthTracking;
