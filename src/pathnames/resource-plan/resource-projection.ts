const main = {
    name: 'Resource Projection',
    path: '/resource-projection'
};
const configuration = {
    name: 'Configuration',
    path: '/resource-projection-configuration'
};

const resourceProjection = {
    main,
    configuration
};

export default resourceProjection;
