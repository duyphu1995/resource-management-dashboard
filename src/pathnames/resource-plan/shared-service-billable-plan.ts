const main = {
    name: 'Shared Service Billable Plan',
    path: '/shared-service-billable-plan'
};

const detail = {
    name: 'Shared Service Billable Details',
    path: '/shared-service-billable-details'
};

const add = {
    name: 'Add Shared Service Billable Plan',
    path: '/add-shared-service-billable-plan'
};

const sharedServiceBillablePlan = {
    main,
    detail,
    add
};

export default sharedServiceBillablePlan;
